// API Configuration
// Set this to false when you have a real backend API
export const USE_MOCK_API = true;

// Your API base URL - replace with your actual API endpoint
export const API_BASE_URL = 'https://your-api-domain.com';

// API endpoints
export const API_ENDPOINTS = {
  GAMES: '/api/games',
  USER_STATS: '/api/user/stats',
  CREATED_GAMES: '/api/games/created',
};

// Mock delay for API calls (in milliseconds)
export const MOCK_API_DELAY = 1000; 